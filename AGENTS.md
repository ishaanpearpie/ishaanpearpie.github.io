This project uses bun, use only bun and not npm or node.

This is a Astro project with TailwindCSS
Therefore never use img src, always use Astro optimized Image component

Always style components using TailwindCSS, never write plain bare CSS

All existing and new colours should always be in oklch

